# 当前任务卡片

## 项目名称

AI 接口测试平台

## 当前阶段

Stage 1：OrderFlow Demo 被测业务系统

## 当前任务

Stage 1 - Task 3：设计订单系统领域模型

## 任务目标

设计 OrderFlow Demo 的核心领域模型，为后续商品、用户、订单、支付、退款等接口实现打好基础。

## 已完成内容

- 已创建任务文档目录。
- 已创建 `orderflow-demo` 项目骨架。
- 已完成 Stage 1 - Task 1 验收。
- 已完成 Stage 1 - Task 2 验收。
- 已创建基础统一响应对象 `ApiResponse`。
- 已创建错误码枚举 `ErrorCode`。
- 已创建业务异常类 `BusinessException`。
- 已创建全局异常处理器 `GlobalExceptionHandler`。
- 已创建部分领域对象：`User`、`Product`、`OrderInfo`、`OrderItem`、`OrderStatus`。

## 当前问题

- Stage 1 - Task 3 本次验收未通过。
- `repository` 包下仍只有 `package-info.java`，缺少仓储接口结构，例如 `UserRepository`、`ProductRepository`、`OrderInfoRepository`、`OrderItemRepository`。
- `OrderInfo` 当前字段更像订单明细，缺少订单主对象应有字段，例如订单 ID、用户 ID、订单状态、总金额、创建时间、支付时间、发货时间、完成时间等。
- `Product` 使用 `double price`，金额字段建议统一使用 `BigDecimal`。
- 部分 getter/setter 命名不规范，后续序列化、框架绑定或 Bean 访问可能出问题，例如 `setPriced`、`setDescriPtion`、`status()`、`setOrderStatus`。
- 当前环境未识别 `mvn` 命令，无法在本机执行 Maven 自动化测试。

## 需要 Codex 做什么

请根据以下要求修改 Stage 1 - Task 3：

- 补充清晰的领域模型：`User`、`Product`、`OrderInfo`、`OrderItem`、`OrderStatus`。
- 让 `OrderInfo` 表达订单主表信息，`OrderItem` 表达订单明细信息。
- 明确订单状态枚举：待支付、已支付、已取消、已发货、已完成、已退款。
- 补充仓储接口结构，例如 `UserRepository`、`ProductRepository`、`OrderInfoRepository`、`OrderItemRepository`。
- 修正字段类型和 JavaBean 命名问题，金额字段使用 `BigDecimal`。
- 不提前实现商品、用户、订单、支付、退款等完整业务流程。

验收通过后，进入 Stage 1 - Task 4：实现商品模块。
